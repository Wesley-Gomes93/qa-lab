# Screenshots for QA Lab

Add screenshots here to showcase the project. Recommended captures:

1. **`pipeline.png`** — GitHub Actions pipeline green (all jobs passed)
2. **`report-unified.png`** — Unified QA Lab report (Cypress + Playwright)
3. **`test-writer.png`** — Test Writer agent generating a test (optional)

After adding images, reference them in the main README:

```markdown
| Pipeline | Unified Report |
|----------|----------------|
| ![Pipeline](../docs/screenshots/pipeline.png) | ![Report](../docs/screenshots/report-unified.png) |
```

Or use absolute paths from repo root: `docs/screenshots/pipeline.png`

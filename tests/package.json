{
  "name": "qa-lab-tests",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test:cy": "cypress run; node scripts/print-summary.js cypress",
    "test:pw": "playwright test; node scripts/print-summary.js playwright",
    "test:all": "cypress run; playwright test; node scripts/print-summary.js all",
    "cypress:open": "cypress open",
    "cypress:run": "cypress run",
    "pw:test": "playwright test",
    "pw:security": "playwright test --project=security",
    "pw:security": "playwright test --project=security",
    "pw:security": "playwright test --project=security",
    "pw:security": "playwright test --project=security",
    "pw:security": "playwright test --project=security",
    "pw:security": "playwright test --project=security",
    "pw:security": "playwright test --project=security",
    "pw:ui": "playwright test --ui",
    "pw:report": "playwright show-report",
    "pw:security": "playwright test --project=security",
    "pw:security": "playwright test --project=security",
    "pw:security": "playwright test --project=security",
    "report:unified": "node scripts/generate-qa-report.js",
    "report:execution": "node scripts/execution-report.js",
    "test": "cypress run",
    "test:report": "cypress run && node scripts/report-to-api.js",
    "test:execution": "cypress run && playwright test && node scripts/execution-report.js",
    "report-to-api": "node scripts/report-to-api.js"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.10.2",
    "@playwright/test": "^1.58.2",
    "cypress": "^15.11.0",
    "cypress-mochawesome-reporter": "^3.8.4"
  }
}
